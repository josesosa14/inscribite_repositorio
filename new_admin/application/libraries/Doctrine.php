<?phpuse Doctrine\Common\ClassLoader,    Doctrine\ORM\Configuration,    Doctrine\ORM\EntityManager,    Doctrine\Common\Cache\ArrayCache,    Doctrine\DBAL\Logging\EchoSQLLogger;class Doctrine {    public $em = null;    public function __construct() {        // load database configuration from CodeIgniter        //require_once APPPATH . 'config/database.php';        // Set up class loading. You could use different autoloaders, provided by your favorite framework,        // if you want to.        //require_once APPPATH.'libraries/Doctrine/Common/ClassLoader.php';        // We use the Composer Autoloader instead - just set        // $config['composer_autoload'] = TRUE; in application/config/config.php        require_once RAIZ . DIRECTORY_SEPARATOR . 'vendor' . DIRECTORY_SEPARATOR . 'autoload.php';        //A Doctrine Autoloader is needed to load the models        $entitiesClassLoader = new ClassLoader('Entities', APPPATH . "models");        $entitiesClassLoader->register();        // Set up caches        $config = new Configuration;        if (ENVIRONMENT == "production") {            $cache = new \Doctrine\Common\Cache\ApcCache;        } else {            $cache = new ArrayCache;        }        $config->setMetadataCacheImpl($cache);        $driverImpl = $config->newDefaultAnnotationDriver(array(APPPATH . 'models/Entities'));        $config->setMetadataDriverImpl($driverImpl);        $config->setQueryCacheImpl($cache);        $config->setQueryCacheImpl($cache);        // Proxy configuration        $config->setProxyDir(APPPATH . '/libraries/proxies');        $config->setProxyNamespace('Proxies');        // Set up logger        //$logger = new EchoSQLLogger;        //$config->setSQLLogger($logger);        $config->setAutoGenerateProxyClasses(TRUE);        // Database connection information        if (ENVIRONMENT == "production") {            $connectionOptions = array(                'driver' => 'pdo_mysql',                'user' => "inscribite_user",                'password' => "tIvxBjA3",                'host' => "200.85.154.113",                'dbname' => "inscribite_base"            );        } elseif (ENVIRONMENT == "local") {            $connectionOptions = array(                'driver' => 'pdo_mysql',                'user' => "root",                'password' => "",                'host' => "localhost",                'dbname' => "admin_inscribite"            );        } else {            $connectionOptions = array(                'driver' => 'pdo_mysql',                'user' => "inscribite_user",                'password' => "tIvxBjA3",                'host' => "localhost",                'dbname' => "inscribite_base"            );        }        // Create EntityManager        $this->em = EntityManager::create($connectionOptions, $config);    }}